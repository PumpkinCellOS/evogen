function test(name, callback) {
    let env = new Object();
    env.name = name;
    env.callback = callback;
    env.callback();
}

function expect(cond) {
    if(!cond)
        test_fail(this.name);
}
