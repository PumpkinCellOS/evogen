function test(name, callback) {
    sys.writeln("Test case: " + name);
    let env = new Object();
    env.name = name;
    env.callback = callback;
    env.callback();
}

function expect(cond) {
    if(!cond)
        test_fail(this.name);
}
